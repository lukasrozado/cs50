#include <cs50.h>
#include <stdlib.h>
#include <stdio.h>

int main (void)
{
    int scores [3];
    for (int i = 0; i < 3; i++)
    {
        printf("%i\n", scores[i]); // If not declared will be showed random numbers in the memory.
    }

}